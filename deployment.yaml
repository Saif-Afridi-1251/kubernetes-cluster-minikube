kind: Deployment
apiVersion: apps/v1
metadata:
  name: webdeployment
spec:
  replicas: 3
  selector:
    matchLabels:
      name: deployment
  template:
    metadata:
      name: testpod
      labels:
        name: deployment
    spec:
      containers:
      - name: web1
        image: nginx
